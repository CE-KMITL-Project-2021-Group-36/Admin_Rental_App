import 'package:flutter/material.dart';

class WithdrawalRequestScreen extends StatelessWidget {
  const WithdrawalRequestScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('คำขอถอนเงิน'),
    );
  }
}
